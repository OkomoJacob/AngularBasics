var mess;
mess = "type string";
// Assert that mess is trsing, not any.
var printMess = mess.endsWith('i');
var alaternativePrint = mess.endsWith('e');
console.log(printMess);
// console.log(alaternativePrint);
